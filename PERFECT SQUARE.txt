#include <stdio.h>
#include <math.h>

int main()
{
    int n, s;

    printf("Enter a number: ");
    scanf("%d", &n);

    s = sqrt(n);

    if(s * s == n)
        printf("Perfect square");
    else
        printf("Not a perfect square");

    return 0;
}
