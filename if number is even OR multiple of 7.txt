#include <stdio.h>

int main() {
    int num;

    printf("Enter a number: ");
    scanf("%d", &num);

    if(num % 2 == 0 || num % 7 == 0) {
        printf("Number is even or multiple of 7\n");
    }
    else {
        printf("Number is not even and not multiple of 7\n");
    }

    return 0;
}
